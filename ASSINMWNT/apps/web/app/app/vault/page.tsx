import type { Metadata } from 'next';
import VaultPage from '../../../components/app/VaultPage';

export const metadata: Metadata = { title: 'Vault | Omnimise' };
export default function Vault() { return <VaultPage />; }
